struct V3 {
    x: Number,
    y: Number,
    z: Number
}

impl V3 {
    fn From(x: Number, y: Number, z: Number) {
        return V3 {x: x, y: y, z: z};
    }

    fn Inspect(self: V3) {
        log("X", self.x);
        log("Y", self.y);
        log("Z", self.z);
    }
}

V3::Inspect(V3::From(12, 1, 3));