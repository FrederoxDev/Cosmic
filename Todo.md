# Todo: Figure out interpreter design

How will variables be stored in memory. How do we work with `this` and other things like assignment ops (+=, -=, etc..) that need to work on references to variables